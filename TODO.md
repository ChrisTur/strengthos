# TODO

- **Extend `targets`/`cue` exercise metadata beyond Back.** Only the 35 Back
  exercises in `EXERCISE_LIBRARY` (js/data.js) have `targets`/`cue` fields —
  the other ~300 exercises across Chest, Shoulders, Biceps, Triceps, Traps,
  Quads, Hamstrings, Glutes, Calves, Core, and Cardio don't, so the "Add
  Exercise" picker only shows rich coaching guidance for Back movements.
  Writing accurate form cues for ~300 exercises is a lot of content to get
  right in one pass — do it in a reviewable batch (e.g. one muscle group at
  a time) rather than all at once, or decide to drop the fields from Back
  for consistency instead.
